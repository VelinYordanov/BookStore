<div id="bookDetails">
    <h1>{{name}}</h1>
    <img src="data:image/png;base64, {{picture}}" alt={{name}}>
    <p>{{bio}}</p>
    <button id='favorite-author'>Favorite this author</button>
    {{#each books}}
        <h1>{{title}}</h1>
    {{/each}}
</div>